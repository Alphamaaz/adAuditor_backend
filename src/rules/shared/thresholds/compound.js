/**
 * Cross-platform / compound rule thresholds.
 */

export const COMPOUND_BRAND_CANNIBALIZATION = Object.freeze({
  // Minimum Google brand-tagged spend to consider brand search "active".
  MIN_GOOGLE_BRAND_SPEND: 100,
});
